# COMSOL Acoustic Metadiffuser Geometry Generator

This script calls the function geo_generator, which generates COMSOL geometry for an acoustic metadiffuser.

- Step 1: Run LiveLink

- Step 2: run `main.m`
